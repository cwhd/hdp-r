#do HDM calculations

hdp.calculate.all <- function(modelWithWeights) {
  #do it now!
  #for each path in the tree starting at the expert
  #multiply the weights, divide by 100
  #
}

hdp.calculate.expert <- function(modelWithWeights) {
  #calulate results for one of the experts?
}

hdp.calculate.inconsistency() <- function(modelWithWeights) {
  #do the inconsistency calculation
}