###############################################
# generate sliders for expert evaluation
###############################################

sliderRatingUI <- function(id) {
  ns <- NS(id)
  #TODO put the UI here
}

sliderRating <- function(input, output, session) {
  #TODO put code here...
}

